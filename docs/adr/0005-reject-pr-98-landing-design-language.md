# ADR 0005: PR #98 반려 — 랜딩 디자인 언어의 확장 이식 거부

| 항목 | 값 |
|---|---|
| 상태 | 채택 |
| 작성일 | 2026-07-30 |
| 이슈 | - |
| 출처 | 옛 `spec/3-3-DESIGN-DECISIONS.md` §3-3-5 (2026-09-13 분리) |

**배경**

외부 기여자가 [이슈 #97](https://github.com/kangkyunghyun/LinKHU/issues/97)에 대응하는 [PR #98](https://github.com/kangkyunghyun/LinKHU/pull/98)을 제출했다. 시스템·라이트·다크 3단계 테마 선택을 추가하는 PR이었고, 구현 품질과 검증은 충실했다. PR 본문에 30개 테스트 통과, 실제 브라우저에서의 테마 전환·저장·재실행 확인, 여러 폭에서의 반응형 확인, 명도 대비 측정, `prefers-reduced-motion` 대응까지 기록되어 있었다.

문제는 품질이 아니라 **방향**이었다. 이슈 #97과 PR #98은 다크 테마의 시각 언어를 "랜딩 페이지 다크 구역의 따뜻한 배경과 밝은 핑크 강조색"에서 가져오도록 설계되어 있었다. 즉 랜딩의 디자인 언어를 확장 프로그램 UI로 이식하는 방향이었다.

랜딩은 넓은 화면에서 스크롤하며 읽는 홍보 페이지이고, 확장 팝업은 320px 폭에서 즉시 목표를 달성해야 하는 도구다. 두 표면은 목적이 다르므로 시각 언어도 달라야 한다. 이 판단 기준이 당시 문서로 존재하지 않았다.

**대안**

1. 그대로 병합 — 다크 테마를 빨리 확보하고 시각 방향은 나중에 조정
2. 병합 후 후속 PR로 색만 교체 — 기여를 살리면서 방향 수정
3. 반려하고 기존 UI를 유지하는 방향으로 재구현 — 채택

**결정**

3번을 택했다. PR #98을 반려하고, 다크 테마는 추후 기존 확장 UI를 유지하는 방식으로 다시 구현하기로 했다. 반려 시 기여자에게 사유("기존 디자인과 방향성이 다름")와 향후 계획(다크 모드는 추후 반영)을 남겼다. 이슈 #97은 열린 상태로 유지된다.

1번과 2번을 택하지 않은 이유는, 테마 전환 구조가 색 값과 강하게 얽혀 있어 "나중에 색만 바꾸기"가 실제로는 재구현에 가깝기 때문이다. 방향이 다른 구현을 먼저 병합하면 그 위에 쌓인 결정을 되돌리는 비용이 커진다.

**영향**

- **판단 기준이 문서화되었다.** [3-2 확장 우선 원칙](../spec/3-2-DESIGN-UI-RULES.md)이 이 결정에서 나왔다. 앞으로 같은 성격의 PR은 코드 리뷰 전에 이 원칙으로 먼저 판단한다.
- **다크 테마의 요구사항이 스펙으로 확정되었다.** [3-2-5](../spec/3-2-DESIGN-UI-RULES.md)가 3단계 테마 모드, 저장 키와 폴백, 선택 UI 위치, 토큰 값 스왑 방식, 다크 팔레트와 대비 기준을 확정한다. 구현 PR은 그 스펙을 근거로 심사한다.
- 외부 기여자의 작업이 병합되지 않았다. 방향 기준을 문서로 먼저 제공하지 않은 상태에서 기여를 받은 것이 원인이다. 기능 성격의 이슈에는 **디자인 방향과 제약을 이슈 단계에서 명시해야 한다** (SHOULD).
- 이 결정은 "다크 테마를 하지 않는다"가 아니다. 시각 언어를 확장 UI 기준으로 정한 뒤에 한다는 뜻이다.

**참고: 판단이 한 번 뒤집혔다**

초기 리뷰 시점의 판단은 "감사 표시 후 병합하고 메인테이너가 후속 정리"였다. 이후 방향 문제가 색 값 교체로 해결되지 않는다는 점이 분명해지면서 반려로 결론이 바뀌었다. **방향이 어긋난 기여는 "일단 병합하고 나중에 고친다"가 성립하지 않는 경우가 있다**는 것이 이 사례의 교훈이다. 병합 여부를 판단할 때 "나중에 고치는 비용"을 실제로 추정한다 (SHOULD).

**정정: 경쟁 상태에 대한 당시 리뷰 판단은 틀렸다**

당시 리뷰는 제출된 구현의 경쟁 상태 방어를 두고 "`chrome.storage.local.set`은 호출 순서대로 처리되므로 실제로 필요한 것은 저장 실패 시 되돌리기뿐"이라고 기록했다. **이 판단은 틀렸고, 이슈 #97 구현에서 실제 결함으로 증명되었다.**

순서 보장이 무엇을 보장하고 무엇을 보장하지 않는지 구분해야 한다.

| 보장되는 것 | 보장되지 않는 것 |
| --- | --- |
| 쓰기가 호출 순서대로 저장소에 반영된다 | **콜백이 도착하는 순서** |
| 마지막에 쓴 값이 저장소에 남는다 | 늦게 도착한 콜백이 최신 상태를 덮지 않는 것 |
| — | 읽기 응답이 그 뒤에 일어난 쓰기를 반영하는 것 |

쓰기 순서가 지켜져도 다음 두 가지는 그대로 발생한다.

1. **늦게 도착한 실패 콜백이 성공한 최신 저장을 되돌린다.** A(light) 요청 중 B(dark)를 고르면, B가 성공해 저장소가 `dark`가 된 뒤 A의 실패 콜백이 도착해 화면만 되돌려 놓는다. 저장소와 화면이 어긋난다.
2. **늦게 도착한 읽기 결과가 사용자의 명시적 선택을 덮는다.** 초기 읽기가 진행되는 동안 사용자가 테마를 고르면, 뒤늦게 도착한 옛 저장값이 그 선택을 되돌린다.

두 결함 모두 이슈 #97에서 재현 테스트로 확인한 뒤 고쳤다. 해결에 필요한 것은 **단조 증가 요청 번호 하나와 콜백 진입부의 가드 두 줄**이었다.

같은 리뷰에서 "구독자가 하나뿐이니 콜백 하나로 충분하다"고도 판단했다. **이 전제는 이슈 #109에서 바뀌었다** — 팝업의 상태 안내와 설정 페이지의 라디오·안내가 모두 구독자가 되면서 구독자가 둘 이상이 되었다. 단일 슬롯은 나중 등록이 앞 등록을 조용히 덮으므로 해지 가능한 다중 구독으로 바꿨다. 당시 판단이 틀렸던 것이 아니라 전제가 변한 것이다.

따라서 이 사례의 교훈은 "방어가 불필요했다"가 아니라 **"방어의 규모가 과했다"**다. PR #98의 문제는 경쟁 상태를 막으려 한 것이 아니라, 상태 변수 여러 개와 구독자 하나짜리 이벤트 구조로 그것을 한 것이다. 필요한 방어와 과한 방어를 가르는 기준은 다음과 같다.

- **경쟁 상태는 테스트로 재현해 실재를 확인한 뒤 판단한다** (MUST). 재현되면 고치고, 재현되지 않으면 그 근거를 남긴다. "순서가 보장되니 괜찮다" 같은 추론만으로 방어 코드를 걷어내지 않는다 — 이 문서가 그렇게 해서 틀렸다.
- 재현되는 경쟁 상태는 **가장 작은 장치로** 막는다 (SHOULD). 요청 번호 하나로 되는 일에 상태 기계를 만들지 않는다.
- 콜백을 즉시 호출하는 테스트는 순서 문제를 전혀 검증하지 못한다. 콜백을 보류했다가 원하는 순서로 발화시키는 스텁을 쓴다 (SHOULD).
