### Fixes

- 팝업에서 `국제통상금융투자학부`처럼 긴 이름의 뒷부분이 잘려 보이던 것을 고쳤습니다. 카드 한 줄 폭을 실제보다 넓게 잡고 있었습니다. 같은 수정으로 카드 이름 글씨가 조금 커져 읽기 편해졌습니다
- 설정 페이지에서 항목을 드래그할 때 강조되는 영역의 여백이 아래쪽만 떨어져 보이던 것을 고쳤습니다. 네 방향이 같아졌습니다
- 새 버전이 있을 때 팝업 헤더가 두 줄로 밀리던 것을 고쳤습니다. 안내를 `새 업데이트`로 줄였고, 어떤 버전인지는 안내에 마우스를 올리면 보입니다

### Internal

- 여백, 모서리 반경, 그림자, 글자 크기를 `src/theme.css`의 토큰으로 통합. 두 화면에 흩어져 있던 값을 한곳에서 관리하고, 팝업과 설정의 같은 컴포넌트가 같은 값을 쓰도록 정리
- 팝업 카드 한 줄 폭 계산에서 빠져 있던 카드 테두리를 반영하고, 줄바꿈 표의 두 줄이 그 폭에 들어가는지 검사하는 자동 테스트 추가
- 릴리스 절차 정비. 문구 PR을 태그보다 먼저 머지하는 순서 규칙, Chrome 심사 중 업로드 실패 대응, Firefox 릴리스 노트에서 내부 항목 제외
