# LinKHU

작업 규칙의 원본은 `AGENTS.md` 하나다. 아래 임포트로 그대로 따른다.

@AGENTS.md

문서는 [`docs/spec/`](docs/spec/README.md) 하나다. 배경·요구사항·설계·계약·구현 규약·운영을 번호 계층으로 담고, 결정의 배경은 [`docs/spec/DECISIONS.md`](docs/spec/DECISIONS.md)가 로그로 쌓는다. **번호는 층위이므로 위를 고치면 아래를 전부 훑는다.** 코드를 바꾸기 전에 해당 층의 스펙과 관련 결정을 먼저 확인한다.
