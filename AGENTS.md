# LinKHU Agent Guide

이 문서는 LinKHU 저장소에서 AI Agent와 사람이 같은 방식으로 작업하기 위한 작업 하네스입니다.

하네스는 프롬프트 문장만 잘 쓰는 것이 아니라, AI가 혼자서도 안전하게 일할 수 있는 작업 환경을 설계하는 것입니다. LinKHU에서는 이슈, 브랜치, 문서, 검증 기준, PR 기록을 하나의 하네스로 취급합니다.

## 프로젝트 개요

- LinKHU는 경희대학교 주요 웹서비스 바로가기를 제공하는 브라우저 확장 프로그램입니다.
- 앱은 정적 HTML/CSS/JavaScript 기반이며, 확장 프로그램 소스는 `src/` 아래에 있습니다.
- 지원 사이트 목록은 `src/data.js`의 `MASTER_SITE_LIST`가 단일 소스입니다.

## 작업 시작 순서

모든 코드 변경은 다음 순서를 따릅니다.

1. GitHub Issue를 만든다.
2. 최신 `main`을 기준으로 이슈 번호가 포함된 브랜치를 만든다.
3. 변경 범위를 이슈 내용에 맞게 좁게 유지한다.
4. 변경 후 검증 결과를 남긴다.
5. PR을 만들고 관련 이슈를 `Close #이슈번호`로 연결한다.

예시:

```text
Issue: [FEAT] 공통 서비스 바로가기 추가
Branch: feat/9-add-global-support-links
Commit: feat: 공통 서비스 바로가기 추가
PR: feat: 공통 서비스 바로가기 추가
```

## 하네스 운영 루프

작업은 한 번에 완벽하게 끝내는 방식이 아니라, 다음 6개 축을 작게 반복하며 개선합니다.

1. 구조: 파일 위치, 책임, 변경 범위를 먼저 확인한다.
2. 맥락: AI가 알아야 할 프로젝트 규칙과 현재 이슈의 배경을 제공한다.
3. 계획: 무엇을 바꿀지, 어떤 기준으로 끝났다고 볼지 정한다.
4. 실행: 계획한 범위만 구현한다.
5. 검증: 완료 기준을 명령 또는 수동 확인으로 검증한다.
6. 개선: 반복되는 실수나 작업은 문서, 체크리스트, 스크립트로 흡수한다.

반복되는 작업을 세 번 이상 하게 되면 자동화나 문서화 후보로 본다. 예를 들어 사이트 데이터 검증을 매번 수동으로 수행하게 되면 `validate:data` 스크립트 추가를 별도 이슈로 만든다.

## 이름 규칙

- 이슈 제목은 기존 패턴을 따른다.
  - 기능: `[FEAT] ...`
  - 버그: `[BUG] ...`
- 브랜치 이름은 `type/issue-number-short-title` 형식을 사용한다. `type`은 `feat`, `fix`, `chore`, `docs` 등 Conventional Commit 타입을 따른다.
  - 예: `feat/11-project-work-harness`
  - 예: `fix/1-drag-link-opens-icon`
- 커밋과 PR 제목은 Conventional Commit 형식을 따른다.
  - 예: `feat: 단축키 기능 추가`
  - 예: `fix: 설정 페이지 자동 스크롤 기능`
  - 예: `chore: 프로젝트 작업 하네스 설정`

## 안전 규칙

- 사용자가 만든 변경사항을 되돌리거나 덮어쓰지 않는다.
- 작업 전 `git status --short --branch`로 현재 브랜치와 변경사항을 확인한다.
- 현재 작업과 무관한 파일은 수정하지 않는다.
- 이미 열려 있거나 진행 중인 브랜치의 목적과 다른 변경은 새 이슈/브랜치로 분리한다.
- 파괴적인 명령은 명시적 요청 없이는 실행하지 않는다.
- 삭제, 배포, 외부 발송, 권한 변경처럼 되돌리기 어려운 작업은 실행 전에 사용자의 확인을 받는다.
- 가능한 한 되돌릴 수 있는 단위로 작업한다. 큰 변경은 작은 커밋, 작은 PR, 명확한 검증 기준으로 나눈다.

## 맥락 관리 규칙

- `AGENTS.md`는 프로젝트 전체 규칙만 담는다.
- 특정 기능, 배포 절차, 데이터 정책이 길어지면 별도 문서나 스크립트로 분리하고 `AGENTS.md`에서는 링크나 요약만 남긴다.
- 오래된 규칙은 남겨두지 않는다. 현재 코드와 맞지 않는 지침은 하네스 품질을 떨어뜨리므로 정리한다.
- 작업 세션이 길어져 맥락이 비대해지면 현재 목표, 완료된 일, 남은 검증, 열린 위험을 요약한 뒤 이어간다.

## 계획 규칙

- 모호한 요구사항은 구현 전에 의도와 완료 기준을 명확히 한다.
- 단순 변경은 바로 진행하되, 여러 파일이나 운영 절차를 건드리는 변경은 짧은 계획을 먼저 세운다.
- 완료 기준은 측정 가능하게 적는다.
  - 좋은 예: `git diff --check` 통과, 데이터 중복 id 0개, 팝업에서 새 항목 표시 확인
  - 피할 예: 잘 동작함, 보기 좋아짐, 문제 없어 보임
- 계획과 실행을 분리한다. 계획 없이 크게 만들고 나중에 검수하는 흐름은 피한다.

## 실행 규칙

- 단순하고 독립적인 작업은 한 에이전트가 끝까지 처리한다.
- 병렬 작업은 읽기 조사나 서로 다른 파일 소유권이 분명할 때만 사용한다.
- 동일 파일을 여러 흐름에서 동시에 수정하지 않는다.
- 새 도구, 스크립트, 문서 구조는 실제 반복 비용을 줄일 때만 추가한다.

## 데이터 변경 규칙

`src/data.js`를 수정할 때는 다음 항목을 확인한다.

- `id`가 중복되지 않는다.
- `id`, `name`, `url`, `imgSrc`, `category`가 모두 존재한다.
- `category`는 `공통`, `단과대`, `학과` 중 하나다.
- `imgSrc`는 확장 프로그램 루트인 `src/`를 기준으로 한 상대 경로이며, 실제 파일이 존재해야 한다.
- `http://` URL은 가능한 경우 `https://`로 확인하되, 사이트가 HTTPS를 지원하지 않으면 유지한다.

현재 별도 검증 스크립트가 없으므로, 데이터 변경 PR에는 확인 결과를 PR 본문에 적는다.

## 수동 검증 기준

변경 유형별로 다음을 확인한다.

- 사이트 추가/수정: 팝업과 설정 페이지에서 항목이 정상 표시되는지 확인한다.
- 팝업 로직 변경: 클릭, Ctrl/Cmd 클릭, 휠 클릭, 숫자 단축키 동작을 확인한다.
- 설정 페이지 변경: 드래그 앤 드롭, 저장, 새로고침 후 순서 유지 여부를 확인한다.
- Manifest 변경: Chrome/Firefox/Whale 호환성과 권한 범위를 확인한다.

검증은 결과물을 만든 관점과 평가하는 관점을 분리한다. 구현자가 스스로 확인하더라도, PR 본문에는 무엇을 기준으로 어떻게 확인했는지 독립적으로 재현 가능하게 적는다.

## 개선 규칙

- 같은 설명을 반복하면 문서로 만든다.
- 같은 검증을 반복하면 스크립트로 만든다.
- 같은 실수를 반복하면 규칙이나 체크리스트로 만든다.
- 필요 없어진 규칙, 도구, 문서는 제거한다. 하네스는 쌓기만 하는 것이 아니라 단순하게 유지해야 한다.
- 좋은 신호:
  - 같은 말을 두 번 하지 않는다.
  - 실수가 규칙이나 자동화로 흡수된다.
  - 불필요한 단계가 줄어든다.
- 나쁜 신호:
  - 검증에 시간이 점점 오래 걸린다.
  - 시킨 결과가 기대와 자주 어긋난다.
  - 규칙과 문서가 길어지지만 실제 작업에는 도움이 되지 않는다.

## PR 작성 기준

- PR 본문은 저장소의 PR 템플릿을 사용한다.
- 작업 사항은 체크리스트로 완료 상태를 표시한다.
- 테스트 결과에는 실행한 명령 또는 수동 검증 내용을 구체적으로 적는다.
- 데이터 변경이 있으면 데이터 검증 결과를 포함한다.
- 리뷰 포인트에는 확인받고 싶은 판단 지점을 남긴다.
